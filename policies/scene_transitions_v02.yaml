initialized: [visual_planning]
visual_planning: [visual_review_required]
visual_review_required: [visual_approved, visual_planning]
visual_approved: [geometry_pending]
geometry_pending: [geometry_review_required]
geometry_review_required: [geometry_approved, geometry_pending]
geometry_approved: [blockout_pending]
blockout_pending: [lookdev_pending]
lookdev_pending: [render_pending]
render_pending: [render_review_required]
render_review_required: [visual_revision, final_review_required]
visual_revision: [lookdev_pending, render_pending]
final_review_required: [approved, visual_revision]
approved: [delivered]
delivered: []
