# Phase 7–10 execution plan

User-authorized continuation from accepted S11 (change-012). The original phased plan controls ordering and gates; contract-govern-skil controls each evolution. Existing uncommitted S11 work is preserved. Development uses a new branch in the current checkout because the accepted source and local Blender evidence live here; governance runs in the protected source projection.

Each phase: add requirement/claims, name Router insertion, add/run failing tests, impact, implement, register artifacts/tests/version, validate, contract-test, full active regression, actual representative operation, independent review, accept. Preserve original tests and failure logs. Do not start the next production phase before acceptance.

## Phase 7 / S12 — Visual Critic

Impact: preview metadata/current documents → read-only critic context → validated review submission. Insert Step 7V after Step 6V. Codex views the actual image; runtime never invents an image diagnosis.

Files: new runtime/visual_review.py, prompts/render_critic.md, docs/v02/visual-review.md and new critic tests/fixtures; extend ManifestManager/CLI and the existing style rubric. Reuse visual_review schema and state edges; no new shader operations.

Tests: context read-only, valid submission, stale/tampered/wrong image/metadata/scene, failed preview, invalid scopes/categories, immutable review and unchanged approvals, legacy/mode isolation. Cross-document validation must not reuse the obsolete pre-completion packet manifest hash.

Gate: actual fixed deliberately uniform/glossy Blender fixture, independent Codex observations with structured recurring diagnoses; actual formal preview review persisted. Scores are subjective workflow aids, never objective measurements or approval.

## Phase 8 / S13 — Controlled Revision

Impact: current review → bounded revision_plan → newer visual plan revisions → guarded Blender preview. Insert Step 8V after 7V and return to 6V/7V.

Files: revision_controller, Manager/CLI, small optional contract fields required by supported visual adjustments, semantic worker integration and new tests. Six allowed actions only: roughness variation, lighting intensity/direction, fog, camera framing, exposure. Geometry and expensive regeneration require separate review.

Tests: exact recommendation linkage, supported scope/direction/amount, default three-pass ceiling including failures, unchanged geometry/source/approval histories, no repeated application, stale/tampered/replayed plan rejection, no unintended regeneration, legacy defaults preserved.

Gate: real MCP Preview 00 → observed diagnosis → persisted revision → adjusted Preview 01; metadata and histories trace each pass. No automatic artistic approval.

## Phase 9 / S14 — HY3D semantic integration

Keep SSH/HTTP transport and old endpoints. Add generate_geometry/generate_surface_source semantics, preserve raw texture as immutable source and apply the existing style materials independently. No implicit expensive retry or invented license declarations.

Tests: old and new API compatibility, capability/rights failures before generation, raw geometry/texture preservation, source role and identity, unchanged transport and style-resolved geometry.

Gate: compare a real textured HY3D asset before/after style resolution. If no usable real asset/rights exist, report the precise missing input; test doubles cannot fulfill this real comparison.

## Phase 10 / S15 — industrial_acg_v1 refinement

Distill selected role-scoped references into style_signature.yaml covering geometry, surfaces, palette, roughness, weathering, light, fog, depth, composition, emissive use and detail density. References may be existing original calibration/preview images when no user reference is supplied; label that scope.

Refine the current overcast profile and station composition first; no requirement to add sunset/interior/wet/snow. Preserve existing profile compatibility and resources/history. Any resource change requires a real calibration render and regression.

Gate: actual industrial station with differentiated material response, heterogeneous source consistency, readable foreground/midground/background and directional light across multiple preview iterations. Inspect images, record limitations and reference/Codex review. Phase 11/12 are outside this request.
