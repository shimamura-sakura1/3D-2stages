# S4 跨平台维护与 Stage 1 验证记录

此文记录开发过程与语义审查；正式接受状态只由 `changes/` 中外部工具生成的记录决定。

## 需求与修改范围

按用户指定的 `/Users/tachibanakanade/contract-govern-skil` 的 README、SPEC、EVOLVE 和 TEST_POLICY 执行。新增 `REQ-PORTABLE-001`，保留既有需求、测试 ID、测试文件和断言。`AGENTS.md` 保存项目维护规范、隔离环境约束与证据边界；生产 Router 仍是 `SKILL.md`。

Router Step 1 读取 `docs/platforms.md`，使用公开 CLI 的 `doctor` 了解当前主机。Step 5 明确 Blender 4.2 下限、MCP 按宿主验证、迁机后重新准备操作包。默认模式、MCP 优先、Manifest 唯一写入入口和审批要求保持不变。

实现包括跨宿主相对路径检查、显式 batch 的 Blender 发现、工作者版本门禁、当前 Python 诊断，以及治理框架位置与解释器选择。源码投影增加项目记忆。已有独立 `scripts/smoke_hy3d.py` 只补充登记和实际依赖关系，未修改其源码，也不把它当作正式 Stage 1。

## 测试先行与修复证据

- 改实现前，新增跨平台测试首次运行出现 18 个失败、1 个通过，失败来自尚未实现的路径处理、诊断、发现和版本门禁。原始输出在本机忽略目录 `.deps/govern-s4/test-first.txt`。
- 先执行指定框架的 impact；初始范围包括 Router、操作指南、CLI、工作者、Stage 2、路径工具和治理入口，输出在 `.deps/govern-s4/impact-before.json`。新增文件同步登记依赖和测试覆盖。
- 回归暴露历史测试要求缺失 Blender 的错误包含 `not found`，修复实现消息保留该协议，没有改历史断言。
- 新 C 路径测试的自建来源 fixture 曾使用 Schema 不接受的 `title` 字段，按原 Schema 改为 `creator` 和 `original_url`；没有放宽生产许可或来源契约。
- 开发目录的真实 `.env` 使历史“未配置 SSH”测试的前提不成立，并触发只读连接诊断。正式回归采用既有治理源码投影排除 `.env`，保留真实部署文件和原测试。本次未对真实网关发起生成。
- 新测试覆盖正例、原默认行为、结构化拒绝、实际文件和回环 HTTP。Stage 1 覆盖 A、B、C shape、C textured，每条完整流程包含返工、旧版本保留、未批准交付拒绝、批准交付和超限返工拒绝。

初轮全部 17 组 active 治理测试通过，运行契约的声明检查和实际 I/O 均通过。之后补充 POSIX 绝对路径和父目录拒绝边界；最终回归、历史指纹和接受结果以正式工具输出为准，不以本段工作记录代替验收。

接受预演进一步发现 Git checkout 换行转换：`change-001/002/003.json` 从 CRLF 变为 LF，但分别恢复 CRLF 后与后续记录引用的 SHA256 精确一致；4 个旧 JSON fixture 同样可恢复到已接受摘要。新增 C6 与 5 个测试，先确认全部因恢复函数缺失而失败，再执行治理入口 impact。只在临时投影恢复有历史摘要证明的原始字节，无法匹配的改动拒绝，真实历史文件不变。证据位于 `.deps/govern-s4/newline-test-first.txt`、`newline-impact.json` 及最终报告的恢复列表；这不是放宽外部框架的哈希校验。

## 代表性真实操作与语义审查

按 Router 在 `.deps/govern-s4/representative-project` 建立独立技术样例，使用仓库原始 CC0 长椅，执行计划批准、获取、未批准交付拒绝、返工、测试审核输入和资产交付。检查交付模型摘要与结果一致，Stage 1 曾到达 `complete`。测试审核输入只用于验证状态机，不代表对用户成品的美术批准。

显式选择 batch 验证本次发现逻辑与工作者：未传 Blender 路径，自动找到本机 Blender 4.5.13，实际导出 `scene.blend`、`scene.glb`、`preview.png` 和摘要，正式 Stage 2 记录为 `built`。查看预览确认站台、铁轨和长椅可见，保留最终审核待办。操作记录在 `.deps/govern-s4/representative.json`，产物在样例的 `stage2/scene/build-075a130ba46b/`。

静态 validate 当次无 warning。Windows 安装目录的数字正则只排序文件夹版本，不做自然语言分类；路径检查拒绝跨系统绝对路径和逃逸。Blender 显式配置错误不会静默回退，MCP 不触发 batch 发现。诊断仅报告发现结果，不据此认定版本合格、服务在线或艺术质量通过。

## 证据限制与真实测试待办

- Windows 通过宿主目录和路径语法模拟，尚未在 Windows 实机运行；本机 Blender 证据不外推到其他安装。
- 本地 HTTP B/C 流程验证使用原始三角形 fixture 的真实协议和文件流，不是远程 GPU 结果。
- 既有 `projects/hy3d-smoke` 只证明此前一次直接 shape 客户端调用成功，未经过完整 Stage 1。
- 用户已选择沿用该目录的 `input/demo.png`，并说明许可稍后补充。已在 `projects/stage1-full-validation` 准备独立 shape/textured 任务、相同摘要的参考图、库和网关配置，保留人工审核且未发起推理。具体就绪信息见该项目 `readiness.json`。
- 参考图权利与生成结果来源许可未核实，不填写虚假的 `license_verified: true`。现有正式输出政策只接受已核实的 CC0 / CC BY；真实端到端完成和 `REQ-LIVE-001` 仍不能标记通过。

正式回归与 accept 使用专用 Conda `two-stage-3d` 和指定框架源码，报告保存在 `.skillctl/reports/`；不安装依赖到 base、既有环境或外部框架。
